import React from 'react';
import {VerificationHistory} from '../entities/verification/ui';

const VerificationHistoryScreen = () => {
  return <VerificationHistory />;
};

export default VerificationHistoryScreen;
